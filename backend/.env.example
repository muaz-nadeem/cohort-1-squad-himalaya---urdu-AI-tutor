# ── Groq (LLM + Embeddings + Whisper STT + Vision) ──
GROQ_API_KEY=your_groq_api_key
# Optional: override the default LLM / vision models (tutor + textbook RAG)
# GROQ_LLM_MODEL=llama-3.3-70b-versatile
# GROQ_VISION_MODEL=qwen/qwen3.6-27b
# Optional: MCQ PDF ingest only (text→JSON + scanned-page vision)
# GROQ_MCQ_TEXT_MODEL=llama-3.1-8b-instant
# GROQ_MCQ_VISION_MODEL=qwen/qwen3.6-27b

# ── Uplift AI (Orator TTS) ──
UPLIFT_API_KEY=your_uplift_key
UPLIFT_VOICE_ID=v_8eelc901

# ── Supabase (Postgres + pgvector) ──
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_service_or_anon_key

# ── Server ──
PORT=8000
# Comma-separated list of allowed origins for CORS (frontend URL)
CORS_ORIGINS=http://localhost:3000
